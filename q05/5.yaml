apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    run: limit
  name: limit
  namespace: qq3
spec:
  containers:
    - image: redis
      name: limit
      resources:
        requests:
          memory: "500Mi"
          cpu: "0.5"
        limits:
          memory: "1Gi"
          cpu: "1.0"
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
